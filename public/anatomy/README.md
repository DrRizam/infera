# Muscle images for the Anatomy quiz

Drop one image per muscle here, named exactly as the `image` field in
src/data/muscles.js (e.g. `supraspinatus.png`). Any web format works
(png/jpg/webp/svg). Until a file exists, that muscle's "identify" question
shows a placeholder.
